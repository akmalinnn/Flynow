package com.km6.flynow.presentation.checkout.checkout_pemesan

import android.os.Bundle
import androidx.lifecycle.ViewModel

class BiodataPemesanViewModel (
    extras: Bundle?,

    ) : ViewModel() {
        //fun getUserID() = pref.getUserID()
       // fun getProfile(id: String) = profileRepository.getProfile(id).asLiveData(Dispatchers.IO)
    }